spring.application.name=Journaling App

# Updated MongoDB URI with SSL and TLS settings
spring.data.mongodb.uri=mongodb+srv://abhijandas8_db_user:Mukta1234786@cluster0.rycaqup.mongodb.net/journaldb?retryWrites=true&w=majority&tls=true&tlsAllowInvalidHostnames=true

# Server configuration
server.port=8080

# Static resource configuration
spring.web.resources.static-locations=classpath:/static/
spring.mvc.static-path-pattern=/**

# CORS configuration (if needed)
cors.allowed-origins=http://localhost:3000,http://localhost:8081,http://127.0.0.1:5500

# MongoDB specific settings to handle SSL issues
spring.data.mongodb.auto-index-creation=true

# Logging for debugging (optional)
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.com.mongodb=DEBUG
